// @flow
import { typo, color, COLOR } from '../styles'

export default () => {
  const tabTheme = {
    flex: 1,
    backgroundColor: '#FFF'
    // backgroundColor: COLOR.blue
  };

  return tabTheme;
};
